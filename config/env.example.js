/**
 * env.example.js – Copy this file to env.js and fill in your keys.
 * env.js is git-ignored so secrets stay local.
 */

const ENV = {
  WEATHER_API_KEY: 'YOUR_API_KEY_HERE'
};

export default ENV;
